# Chave pública do Mercado Pago (não é secreta — pode ser exposta no navegador),
# usada pelo SDK do MP.js para tokenizar o cartão no checkout embutido.
# A chave PRIVADA (access token) fica só no backend/.env, nunca aqui.
VITE_MERCADOPAGO_PUBLIC_KEY=
