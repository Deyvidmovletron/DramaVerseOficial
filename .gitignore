# Backend (Python)
backend/venv/
backend/__pycache__/
backend/**/__pycache__/
backend/*.egg-info/
backend/.env
backend/data/
backend/media/**/*
!backend/media/**/.gitkeep

# Frontend (Node)
frontend/node_modules/
frontend/dist/
frontend/.env
frontend/*.local

# Deploy (docker compose / stack)
.env

# Deploy de produção (Swarm) — contém segredos reais em texto puro
# (SECRET_KEY, credenciais SMTP, tokens do Mercado Pago, senha do admin).
# NUNCA remover esta linha nem forçar `git add -f` dentro de DEPLOY/.
DEPLOY/

# Editor / OS
.vscode/
.idea/
.DS_Store
Thumbs.db
