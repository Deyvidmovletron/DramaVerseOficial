# Lido automaticamente pelo `docker compose` pra preencher build.args do serviço
# frontend (docker-compose.yml) — a chave pública do Mercado Pago precisa ir
# embutida no bundle estático em tempo de build. Não é secreta.
# Segredos de runtime do backend continuam em backend/.env (não aqui).
VITE_MERCADOPAGO_PUBLIC_KEY=
